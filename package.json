{
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "homepage": "https://<Username>.github.io/projects/pomodoro",
  "scripts":{
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build"
}  
}
